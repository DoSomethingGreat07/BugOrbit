# RocketRide AI Tools

This directory is reserved for tool adapters used by the orchestration layer.

Planned adapters:
- `trace_fetcher.py` for Datadog and SigNoz trace ingestion
- `neo4j_query.py` for graph retrieval
- `incident_search.py` for vector search and postmortem retrieval
